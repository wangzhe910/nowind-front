const form = {
  'form.loginTitle': 'Login',
};

const baseLayout = {
  'baseLayout.loginTitle': '请先登录',
  'baseLayout.loginText':
    'In order to continue working with the application you have to re-logon.',
};

const pageIndex = {
  'pageIndex.title': 'hello',
};

export default {
  ...form,
  ...baseLayout,
  ...pageIndex,
};
